"""우체부의 명령 계약 — 열거형의 단일 출처만 담는다 (ADR-002 D1·D2).

D1의 새 경계 문구는 이렇다.

> 우체부는 프로젝트 상태를 **해석하지 않는다** — 무엇을 넣을지는 결정하지 않고,
> 이미 결정된 문자열을 지정된 주소에 넣고 들어갔는지 확인한다.

그 경계를 코드로 강제하는 검사 둘이 `test_layout.py`에 있다.
① 우체부는 `skills/dev-run/`의 어떤 모듈도 임포트하지 않는다
② 명령 열거형은 이 패키지의 `commands.py` **한 곳에만** 선언된다
"""
